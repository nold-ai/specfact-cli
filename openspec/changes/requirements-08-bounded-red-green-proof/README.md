# Requirements 08: Bounded Red-Green Proof

This change defines a protocol for proving one bounded historical statement by replaying exact selectors at an explicit red commit and the current head. It replaces static pytest/Python dependency-closure inference with an explicit Git mutation policy.

Planning only: no runtime behavior is implemented on this branch.
