## 1. Worktree and readiness

- [ ] 1.1 Create a dedicated `codex/workflow-01-turn-orchestration` worktree from current `origin/dev`; keep the primary checkout on dev.
- [ ] 1.2 Refresh/read back issue type, parent, assignee, labels, project/status and both dependency directions; coordinate concurrent ownership before implementation.
- [ ] 1.3 Revalidate scope and strict OpenSpec; use effective governance or an explicit owner-authorized lean-evidence exception. No blanket full-R09 prerequisite.

## 2. Specification and regression cases

- [ ] 2.1 Confirm the adoption scenarios and paired module interfaces against current core discovery; preserve existing ownership in INTEGRATION.
- [ ] 2.2 Add projection tests for source/destination-only edits, deletion, renderer changes, tracked ignore exceptions, collisions and unsafe paths; observe relevant failures before implementation.
- [ ] 2.3 Add index/worktree/range, missing-tool, independent-gate and source/index no-mutation tests; observe meaningful failures before adapter edits.
- [ ] 2.4 Record concise failing/passing evidence under effective governance; use TDD_EVIDENCE.md if still required. Reuse normal test output, without adding a history protocol.

## 3. Repository projection and check-only adapters

- [ ] 3.1 Implement the canonical projection manifest/renderer and prefixed thin wrappers; ensure only one active native registration per harness.
- [ ] 3.2 Add check-mode local/CI coverage for source, destination, renderer and manifest changes, including deletion; repair ignore exceptions narrowly.
- [ ] 3.3 Implement repository gate configuration and check-only adapters; keep tracked generation, formatting and staging in explicit preparation/fix paths.
- [ ] 3.4 Add the uncovered current GitHub governance checks and optional wiki follow-up integration without widening product requirements.
- [ ] 3.5 Open a bounded projection/adapter PR once reviewable; run relevant gates and merge only through normal reviewed integration.

## 4. Signed adoption and pilot

- [ ] 4.1 Before selecting/adopting #483, validate exact signed #481 archive/manifest/payload identities, schema-v3 current_execution and compatibility with actual core/workflow versions; exercise the installed combination with existing representative fixtures. Missing/unsigned/incompatible/v2-only producers are not ready. Keep projection independent.
- [ ] 4.2 Integrate the reviewed adoption slice before trusted pilot execution; preserve existing required checks and keep R09 policy migration under core #740.
- [ ] 4.3 Run the representative pilot across both repositories; prove fresh-session verification, no source/index mutation and independent failure propagation.
- [ ] 4.4 Enable bounded local repair only after verification acceptance; enable opt-in PR automation only after its paired release/recovery scenarios pass. Test rollback of configuration and module pin together.

## 5. Quality, documentation and delivery

- [ ] 5.1 Run scenario-derived tests and applicable contract, lint/type, security, signature, review and strict OpenSpec gates; resolve findings under effective policy. New public APIs require icontract/beartype contracts.
- [ ] 5.2 Update contributor/agent guidance, CLI reference and generated command docs explicitly; check docs ownership, navigation and real cross-site permalinks.
- [ ] 5.3 Apply semver/changelog/signing only for implementation assets actually changed; keep registry/version compatibility truthful.
- [ ] 5.4 Update issue source tracking and paired wiki mirrors, then open/update the final implementation PR to dev with actual validation and remaining release prerequisites.
- [ ] 5.5 After reviewed integration and completion, archive natively with `openspec archive workflow-01-turn-orchestration`; clean the implementation worktree only after merge. Do not archive this planning proposal as implemented.

Implementation is unstarted. Split each checkbox into bounded work sessions of at most two hours if its observed scope exceeds that size; keep test-before-code ordering within every slice.
