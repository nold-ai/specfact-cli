{
  "mcpServers": {
    "context7": {
      "command": "npx",
      "args": [
        "envmcp",
        "npx", 
        "-y", 
        "@upstash/context7-mcp",
        "--api-key", "${CONTEXT7_API_KEY}"
      ]
    }
  }
}