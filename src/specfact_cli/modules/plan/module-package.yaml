# SpecFact CLI module package manifest.
name: plan
version: "0.27.0"
commands:
  - plan
command_help:
  plan: "Manage development plans"
pip_dependencies: []
module_dependencies:
  - sync
tier: community
core_compatibility: ">=0.28.0,<1.0.0"
