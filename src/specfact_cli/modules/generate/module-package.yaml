# SpecFact CLI module package manifest.
name: generate
version: "0.27.0"
commands:
  - generate
command_help:
  generate: "Generate artifacts from SDD and plans"
pip_dependencies: []
module_dependencies:
  - plan
tier: community
