# SpecFact CLI module package manifest.
name: validate
version: "0.27.0"
commands:
  - validate
command_help:
  validate: "Validation commands including sidecar validation"
pip_dependencies: []
module_dependencies: []
tier: community
