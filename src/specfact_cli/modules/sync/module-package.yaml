# SpecFact CLI module package manifest.
name: sync
version: "0.27.0"
commands:
  - sync
command_help:
  sync: "Synchronize external tool artifacts and repository changes (Spec-Kit, OpenSpec, GitHub, ADO, Linear, Jira, etc.)"
pip_dependencies: []
module_dependencies: []
tier: community
