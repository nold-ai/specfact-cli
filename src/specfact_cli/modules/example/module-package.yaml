# SpecFact CLI module package manifest (not GCP Blueprint).
# Schema: name, version, commands (list), optional command_help, pip_dependencies, module_dependencies, tier, addon_id.
name: example
version: "0.1.0"
commands:
  - example
command_help:
  example: "Example module package (discovered from src/specfact_cli/modules/)"
pip_dependencies: []
module_dependencies: []
tier: community
