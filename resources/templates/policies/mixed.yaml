scrum:
  dor_required_fields:
    - acceptance_criteria
  dod_required_fields:
    - definition_of_done
kanban:
  columns:
    In Progress:
      exit_required_fields:
        - qa_status
safe:
  pi_readiness_required_fields:
    - risk_owner
